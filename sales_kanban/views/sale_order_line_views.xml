<odoo>
    <record id="view_sale_order_line_kanban_custom" model="ir.ui.view">
        <field name="name">sale.order.line.kanban.custom</field>
        <field name="model">sale.order.line</field>
        <field name="arch" type="xml">
           <kanban>
                <templates>
                    <t t-name="kanban-box">
                        <div class="o_kanban_card">
                            <div>
                                <strong>Client : <field name="partner_id"/></strong>
                            </div>
                            <div>
                                <span>Produit : <field name="product_id"/></span>
                            </div>
                            <div>
                                <span>Quantité : <field name="product_uom_qty"/></span>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

     <!-- Action -->
    <record id="action_sale_order_line_kanban" model="ir.actions.act_window">
        <field name="name">Ventes par Client</field>
        <field name="res_model">sale.order.line</field>
        <field name="view_mode">kanban</field>
        <field name="context">{}</field>
    </record>
</odoo>
