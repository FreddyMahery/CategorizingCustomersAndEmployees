<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_hr_employee_form" model="ir.ui.view">
        <field name="name">hr.employee.form.employee.category</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group" position="before">
                <group>
                  <field name="employee_category_id"/>
                </group>
            </xpath>
        </field>
    </record>

    <record id="view_employee_filter_inherit" model="ir.ui.view">
        <field name="name">hr.employee.search.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='group_department']" position="after">
                <filter string="Catégorie" name="group_category" domain="[]" context="{'group_by': 'employee_category_id'}"/>
                <separator/>
                <filter string="Top Performers" name="top_performers" domain="[('monthly_sales', '>', 0)]"/>
            </xpath>
        </field>
    </record>

</odoo>
